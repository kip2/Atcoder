import std.stdio;
import std.algorithm;
import std.array;
import std.conv;

string solve() {
    "Your solved code!"
}

void main() {
    auto nums = readln().split().map!(to!int).array;
    
    writeln(solve());
}
